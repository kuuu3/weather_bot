services:
  lidar-weather-bot:
    build: .
    container_name: lidar-weather-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./state.json:/app/state.json
    command: ["python", "weather_monitor.py", "--bot"]
