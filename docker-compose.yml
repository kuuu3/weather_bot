services:
  taiwan_weather_bot:
    build: .
    container_name: taiwan_weather_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./state.json:/app/state.json
    command: ["python", "weather_monitor.py", "--bot"]
